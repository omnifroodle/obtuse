<%= simple_form_for(@witticism) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :quote %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
